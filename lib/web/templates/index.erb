<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Fyber Offers</title>
    <meta name="description" content="Fyber Offers">
    <meta name="author" content="Philipe Farias">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.96.1/css/materialize.min.css">
    <link rel="stylesheet" href="assets/app.css">

    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no"/>
</head>
<body>
    <nav>
        <div class="nav-wrapper">
            <a href="#" class="brand-logo center">Offer Wall</a>
        </div>
    </nav>

    <div class="row">

        <div class="col s12 m4 l3">
            <div class="row messages">
                <% @form.error_messages.each do |error| %>
                    <div class="error message">
                      <i class="small mdi-alert-error"></i>
                      <span><%= error %></span>
                    </div>
                <% end %>
            </div>

            <div id="offers-form" class="row">
                <%= partial :form, locals: { form: @form } %>
            </div>
        </div>

        <div id="offers" class="col s12 m8 l9 valign-wrapper">
            <div class="row">
                <% if @offers.empty? %>
                    <h5 class="valign">No offers available</h5>
                <% else %>
                    <%= partial :offer, collection: @offers %>
                <% end %>
            </div>
        </div>

    </div>

    <script type="text/javascript" src="https://code.jquery.com/jquery-2.1.1.min.js"></script>
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.96.1/js/materialize.min.js"></script>
    <script type="text/javascript" src="assets/app.js"></script>
</body>
</html>
