<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Fyber Offers</title>
    <meta name="description" content="Fyber Offers">
    <meta name="author" content="Philipe Farias">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>

    <!-- try to prevent caching -->
    <meta http-equiv="Cache-Control" content="max-age=0">
    <meta http-equiv="Cache-Control" content="no-cache">
    <meta http-equiv="Cache-Control" content="no-store" />
    <meta http-equiv="Expires" content="Tue, 01 Jan 1980 1:00:00 GMT">
    <meta http-equiv="Pragma" content="no-cache">

    <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
  </head>
  <body>
    <h1>Offer Wall</h1>

    <h2>Fetch offers</h2>
    <div>
      <form accept-charset="UTF-8" action="/" method="post">
        <div class="field">
          <label for="uid">uid</label>
          <input type="text" id="uid" name="uid">
        </div>

        <div class="field">
          <label for="pub0">pub0</label>
          <input type="text" id="pub0" name="pub0">
        </div>

        <div class="field">
          <label for="page">page</label>
          <input type="text" id="page" name="page">
        </div>

        <input type="submit" name="commit" value="Fetch offers">
      </form>
    </div>

    <h2>Listing offers</h2>
    <div id="offers">
      <% if offers.empty? %>
        <p>No offers available</p>
      <% else %>
        <% offers.each do |offer| %>
          <div class="offer">
            <img src="<%= offer["thumbnail"]["lowres"] %>" alt="<%= offer["teaser"] %>"/>
            <h3><%= offer["title"] %></h3>
            <p><%= offer["teaser"] %></p>
            <div class="time-to-payout"><%= "Auszahlung in #{offer["time_to_payout"]["readable"]}" %></div>
            <div class="action">
              <div class="offer-type"><%= offer["offer_types"].last["readable"] %></div>
              <div class="payout-button">
                <a href="<%= offer["link"] %>"><%= "+ #{offer["payout"]}" %></a>
              </div>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>
  </body>
</html>
